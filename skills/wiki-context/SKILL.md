---
name: wiki-context
description: 个人长期 Wiki 认知模型与上下文沉淀准则。当任务涉及用户历史项目、个人技术偏好、重大架构决策时主动查阅 Wiki；当在交互中识别出用户确定的长期目标、偏好或技术选型变更时，调用 wiki_propose 沉淀知识。
---

# Wiki 长期认知模型与持久化准则

## 适用场景与触发契约
1. **查阅（Read/Search）**：
   - 当用户提及特定项目（如 DSH、历史工具链）、询问“我之前为什么选择 X 而不是 Y”、或讨论涉及个人偏好与私有习惯时，在回答前先调用 `wiki_search` 或 `wiki_read` 确认历史共识。
   - 历史决策与用户既有偏好具有最高指导优先级。

2. **提议入库（Propose）**：
   - 严禁将临时调试日志、临时排查猜想、TODO 待办事项提议入库。
   - 必须通过**时效地平线检验**（“3个月后在不相关的全新会话中，该记录是否依然属实且具备指导意义？”）。
   - 仅当识别到以下内容时，调用 `wiki_propose`：
     - 用户明确申明的长期技术倾向（如“我倾向纯本地部署”、“不要依赖外部重型数据库”）。
     - 重大架构决策闭环（如 ADR 结论、选型原因与取舍）。
     - 核心项目阶段状态演进（如项目新里程碑达成、正式变更技术栈）。

3. **终审闭环机制**：
   - 提交 `wiki_propose` 后，在回复中简要告知用户提案已创建，并附上提案 ID 与审批命令（例如：“已为你生成决策沉淀提案 `prop-xxxx`，可通过 `/wiki approve prop-xxxx` 确认合入 Wiki”）。
   - 禁止在未经审批的情况下伪造已合入的假象。
